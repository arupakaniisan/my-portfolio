<!doctype html>
<html lang="ja">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=Public+Sans:wght@300;400;500;600&display=swap"
			rel="stylesheet"
		/>
		<title>Portfolio</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
	:root {
		color-scheme: light dark;
		--background: #f5f4f1;
		--surface: rgba(255, 255, 255, 0.75);
		--surface-strong: rgba(255, 255, 255, 0.95);
		--text-primary: #191715;
		--text-secondary: rgba(25, 23, 21, 0.65);
		--text-inverse: #ffffff;
		--accent: #5570ff;
		--accent-soft: rgba(85, 112, 255, 0.12);
		--border: rgba(25, 23, 21, 0.08);
		--max-width: 1080px;
		--radius-lg: 28px;
		--radius-md: 20px;
	}

	* {
		box-sizing: border-box;
	}

	html,
	body {
		margin: 0;
		font-family: "Public Sans", "Outfit", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
		color: var(--text-primary);
		background: radial-gradient(circle at top, #ffffff 0%, var(--background) 45%, #ebe8e3 100%);
		min-height: 100%;
	}

	body {
		line-height: 1.65;
		font-size: 16px;
	}

	img {
		max-width: 100%;
		height: auto;
		display: block;
	}

	a {
		color: inherit;
		text-decoration: none;
	}

	button,
	.button {
		font-family: inherit;
	}
</style>
